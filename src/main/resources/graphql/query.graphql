type Query {
    getComputerByProcessor(processor: String!): [ComputerDto!]!
}

type ComputerDto {
    id: ID
    name: String
    processor: String
    ram: Int
}

input ComputerDtoInput {
    name: String
    processor: String
    ram: Int
}
